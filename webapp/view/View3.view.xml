<mvc:View controllerName="fiorifreestyle.controller.View3"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns:core="sap.ui.core"
    xmlns:t="sap.ui.table"
    xmlns="sap.m">
    <Page id="page3" title="{i18n>title}">
      <VBox class="sapUiTinyMargin">
        <Label text="One Way: Model → View"/>
        <Input value="{oneway>/user/name}"/>
        <Text text="{oneway>/user/name}"/>
        <Button text="One Way - Adicionar Ponto Final" press="onTestOneWay"/>

        <Text text="--------------------------------------------------"/>

        <Label text="Two Way: Model ↔ View"/>
        <Input value="{twoway>/user/name}"/>
        <Text text="{twoway>/user/name}"/>
        <Button text="Two Way - Adicionar Ponto Final" press="onTestTwoWay"/>

        <Text text="--------------------------------------------------"/>

        <Label text="One Time: Model → View (uma vez)"/>
        <Input value="{onetime>/user/name}"/>
        <Text text="{onetime>/user/name}"/>
        <Button text="One Time - Adicionar Ponto Final" press="onTestOneTime"/>
      </VBox>
    </Page>
</mvc:View>